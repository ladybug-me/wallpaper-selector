self-hosted-runner:
  labels:
    - skwd-wall-next-ci

config-variables: null

paths:
  .forgejo/workflows/**/*.{yml,yaml}:
    ignore:
      - 'specifying action "https://code\.forgejo\.org/actions/[^" ]+" in invalid format'
